// Placeholder for custom Terra Draw modes.
//
// To add a custom mode:
//   1. Implement a class extending TerraDrawBaseDrawMode (or a built-in mode).
//   2. Export it from this file.
//   3. Register the instance inside DrawController's `modes` array.
//   4. Add the mode key to the `DrawMode` union in DrawController.ts.
export {};
